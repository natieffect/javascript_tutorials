
// java allows the programmets to itterate through codes as long as some condtionas is statified or reached
    // While Loop
        // run the codes until some condtion is true then stops the code
         let variablename = 10;
         let counter = 0;

         while(counter != variablename0){
             console.log(" This is the console function running in the while until the counter reach 10");
             counter++;
         }