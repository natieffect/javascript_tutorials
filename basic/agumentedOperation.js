
// in java sctipt there are operators which can do both matimatical operation and assignment in one step
// ( +=, -=, *=, /= ))

    // Agumented addtions 
        // java sctipt uses ( += ) operator for augmented addtion
         let variableNmae1  = 3;
          vatiableName1 += 1;    // aguments the value with 1 / adds one to the value results 3+1 = 4
    
    // Agumeted substraction
        // java uses ( -+ ) symbol to perform agumented substraction
         let variableName2 =  4
          variableName2 -= 2 // aguments the value with 2 / substract two from the value  results 4-2 = 2
    
    // Agumeted Multiplication
        // java uses ( *= ) symbol to perform augumented multiplicaion 
         let variablename3 = 5;
         variablename3 *= 2   // agumetns the value of variavle with 2 resulting 5 * 2 = 10

    // Agumented Division
        //  java uses ( /= ) symbol to perform agumetented division
         let variableName4 = 4;
         variableName4 /= 2  // aguments the value of the variable with 2 resulting 4 / 2 = 2