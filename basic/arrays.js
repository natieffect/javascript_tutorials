
// we can store several piease of data in one  place using ( java Array )
    // java array can hold diffrent values 
   const variableone = [ 'firstvalue', 'secondvalue', 'third value', 2];

// we can nest one array with onother array
    // nesting one array within onother array is called ( Multi dimentional array)
    let variabletwo = [ ['one',1], ['two',2] ];

// Access array data with index
    // accessing the data using the index 
    let varialethree = variabletwo[0];
    // we can modify the array index data with index
    variabletwo[ 1 ]  = [23];



// Manipulating Arrays with push method 
    // the easier method to add values to rry is to use push method 
    let varaibalefour = [];
    variablefour.push( [ 'first array', 2]);

// Manipulating Arrays with pop method 
     // removes the last element of the array and return removed value
     let value = variabletwo.pop() // return the last element in the array 

// Manipulating Array with shift method
     // removes the first element of the array and return removed value
     let firstvalue =  variabletwo.shift()

// Manipulating Array with Unshift Method
     // addes element at the Beggning of array list 
      secondvalue.unshift([23,4])


    