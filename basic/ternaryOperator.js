// the conditional operator ternary operator can be used as one line if else
// a ? b : c where a is condition b is the code to runwhen the condtion is return true and c is code to run 
// code to run when the condition return false

let comparevalue = 4;

comparevalue > 3 ? true : false;

// multiple condrional operator

comparevalue == 3? "compare equal 3": (compare < 3) ? "compare less than three": "compare greate than three";

